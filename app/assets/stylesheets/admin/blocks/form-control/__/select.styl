.b-form-control__select
  select
    border: none;
    outline: none;
    height: 34px;
    line-height: 34px;
    width: 100%;
    padding: 0 8px;
    font-size: inherit;
    font-familiy: inherit;
    box-shadow: 0 0 5px rgba(#000, 0.05);

    &:focus
      box-shadow: 0 0 5px rgba(#000, 0.1);