.b-page-side
  // background: $black-color;
  height: 100%;
  background: #fff;
  box-shadow: $panel-shadow;
  // color: #fff;
  color: $black-color;

  &_nav
    a
      display: block;
      padding: 0 20px;
      height: 45px;
      line-height: 45px;
      // height: 100%;
      font-weight: 500;
      text-transform: uppercase;

      &.is-active,
      &:hover
        background: $light-gray-color;

      &.is-disable
        opacity: 0.5;
        pointer-events: none;