# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#

default: &default
  adapter:             postgresql
  encoding:            unicode
  reconnect:           true
  pool:             20
  schema_search_path:  public
  host:     <%= ENV['MD_DB_HOST'] %>
  port:     <%= ENV['MD_DB_PORT'] %>
  database: <%= ENV['MD_DB_NAME'] %>
  username: <%= ENV['MD_DB_USER'] %>
  password: <%= ENV['MD_DB_PASS'] %>

development:
  <<: *default
test:
  <<: *default
staging:
  <<: *default
production:
  <<: *default
